<%= form_with url: book_path(book, latest: true), local: false, method: :get do |f| %>
  <%= f.hidden_field :page, value: (params[:page] || 1) %>
  <%= f.hidden_field :status, value: @order %>
  <%= f.submit "もっと見る", class: 'btn btn-outline-warning mb-3' %>
<% end %>
